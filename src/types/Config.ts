export type Config = {
  urlGithubApi: string;
};

export type GitHubConfig = {
  avatarUrl: string;
  name: string;
  email: string;
  location: string;
  repositoriesUrl: string;
  company: string;
  biography: string;
};
